# Dev Workflow

This repo uses Sapling. Use `sl` for common tasks:

```bash
sl status
sl diff
sl commit -m "message"
sl push -r .
```

## Notes

- Sapling is listed in the nix config and can be installed via `rbff doctor`.
